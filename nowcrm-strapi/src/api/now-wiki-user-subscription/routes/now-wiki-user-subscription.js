'use strict';

/**
 * now-wiki-user-subscription router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::now-wiki-user-subscription.now-wiki-user-subscription');
