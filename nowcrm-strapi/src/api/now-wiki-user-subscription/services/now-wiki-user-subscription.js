'use strict';

/**
 * now-wiki-user-subscription service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::now-wiki-user-subscription.now-wiki-user-subscription');
