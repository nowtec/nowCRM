'use strict';

/**
 * composition-prompt router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::composition-prompt.composition-prompt');
