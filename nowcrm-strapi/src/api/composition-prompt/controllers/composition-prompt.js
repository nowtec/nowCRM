'use strict';

/**
 * composition-prompt controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::composition-prompt.composition-prompt');
