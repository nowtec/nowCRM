'use strict';

/**
 * action-normalized-type service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::action-normalized-type.action-normalized-type');
