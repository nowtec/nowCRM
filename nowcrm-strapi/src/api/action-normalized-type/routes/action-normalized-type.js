'use strict';

/**
 * action-normalized-type router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::action-normalized-type.action-normalized-type');
