'use strict';

/**
 * contact-extra-field router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contact-extra-field.contact-extra-field');
