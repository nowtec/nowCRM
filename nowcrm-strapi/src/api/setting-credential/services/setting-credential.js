'use strict';

/**
 * setting-credential service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::setting-credential.setting-credential');
