'use strict';

/**
 * setting-credential router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::setting-credential.setting-credential');
