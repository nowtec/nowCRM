'use strict';

/**
 * composition-template service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::composition-template.composition-template');
