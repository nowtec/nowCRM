'use strict';

/**
 * composition-template controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::composition-template.composition-template');
