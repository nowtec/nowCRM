'use strict';

/**
 * composition-template router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::composition-template.composition-template');
