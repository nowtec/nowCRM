'use strict';

/**
 * unipile-identity controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::unipile-identity.unipile-identity');
