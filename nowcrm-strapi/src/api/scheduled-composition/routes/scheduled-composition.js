'use strict';

/**
 * scheduled-composition router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::scheduled-composition.scheduled-composition');
